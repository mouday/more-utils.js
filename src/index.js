import { fileToBase64, base64ToBlob, getObjectURL, pasteImageFromClipboard } from "./file.js"
import { getImageSize, getImageSizeByUrl } from "./image.js"

export default {
  fileToBase64,
  base64ToBlob,
  getObjectURL,
  pasteImageFromClipboard,
  getImageSizeByUrl,
  getImageSize,
};
